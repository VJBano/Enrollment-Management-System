import { Box, Container, Typography } from "@mui/material";

const Students = () => {
  return (
    <Container maxWidth={false} disableGutters  sx={{backgroundColor:"green"}}>
        <Box >
        <Typography>
        Students
        </Typography>
        </Box>
        
    </Container>
  );
};

export default Students;
